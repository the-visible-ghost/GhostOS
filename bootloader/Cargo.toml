[package]
name = "bootloader"
version = "0.1.0"
edition = "2024"

[dependencies]
log = "0.4.29"
uefi = { version = "0.37.0", features = ["alloc", "global_allocator", "logger", "panic_handler"] }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[toolchain]
targets = ["aarch64-unknown-uefi", "i686-unknown-uefi", "x86_64-unknown-uefi"]
